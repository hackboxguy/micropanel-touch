# Keep the serial console available for recovery, but prevent routine kernel
# notices (including under-voltage messages) from painting over the UI on tty1.
# Boot arguments remain unchanged: cmdline.txt must stay a single line.
kernel.printk = 1 4 1 1
